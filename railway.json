{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --break-system-packages -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python gen.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
